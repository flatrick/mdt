# Codex Rules Source

This directory is reserved for Codex-specific rule files referenced by package manifests.

As of March 9, 2026, the Codex install flow uses `codex-template/skills/` and shared guidance, but no package currently installs `tools.codex.rules`.
