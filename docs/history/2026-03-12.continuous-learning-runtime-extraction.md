# History: 2026-03-12 Continuous-Learning Runtime Extraction

## Summary

This note closes out the runtime-extraction work that had been tracked in the
former repo-root `IMPROVED-SPLIT-CL.md`.

The core architectural change is now in place:

- shared continuous-learning runtime code lives under
  `scripts/lib/continuous-learning/`
- public manual and automatic skill entrypoints are thin compatibility wrappers
- Codex keeps the explicit/manual baseline while optional observer behavior
  remains external and non-hook-based

## What Shipped

- shared runtime modules for context resolution, project detection, observer
  runtime, CLI runtime, Codex manual workflow, and retrospectives
- manual and automatic wrappers that resolve the private runtime from both repo
  and installed layouts
- installed-layout helpers and compatibility coverage for Claude, Cursor, and
  Codex surfaces
- observer/runtime tests that protect the extracted ownership model

## Current Contract Locked In

- project-scoped state lives under `~/.{tool}/mdt/homunculus/<project-id>/`
- the project registry stays at `~/.{tool}/mdt/homunculus/projects.json`
- public wrappers remain the stable user-facing entrypoints
- no public `continuous-learning-core` skill exists

## Remaining Follow-Up

The extraction itself is no longer active roadmap work. Remaining follow-up now
lives in normal repo planning:

- detached-process lifecycle management for background helpers
- validation of weekly retrospective usefulness
- deferred VCS/non-git and `.self/` follow-ups already tracked in `BACKLOG.md`
