# History: 2026-03-12 Roadmap Rebaseline

## Summary

`NEXT-STEPS.md` had accumulated a mix of active work, completed milestones, and
historical design notes. This pass trimmed it back to active roadmap items only.

## Completed Work Removed From Active Roadmap

These items were no longer future work and were removed from `NEXT-STEPS.md`:

- package-manifest validation and explicit `extends` / `requires` handling
- skill dependency/runtime metadata handling in installer and validation flows
- deeper Claude workflow smoke coverage
- Cursor compatibility suites for agent and IDE bridge behavior
- continuous-learning runtime extraction as the active architecture change

## Current Source Of Truth Split

After the rebaseline:

- `NEXT-STEPS.md` contains only active near-term work
- `BACKLOG.md` contains deferred or explicitly post-v1 items
- `docs/package-manifest-schema.md` is the current manifest contract
- `docs/history/` holds completed migration/design records
